## General Guidelines

- Prefer local development environment instead of docker for testing, formatting, and linting.
- while adding new features write failing test cases first.
- Use `uv run` prefix for python commands.